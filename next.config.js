/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed for now
  // You can add image domains here later if you use next/image with external URLs
};

module.exports = nextConfig;
